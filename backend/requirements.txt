fastapi==0.136.3
uvicorn==0.49.0
websockets==13.1
sqlalchemy==2.0.36
aiosqlite==0.20.0
# PostgreSQL drivers (production runs on PostgreSQL): asyncpg is the async
# driver the app uses at runtime; psycopg2-binary is the sync driver used by
# backend/migrations/db_utils.py to run schema migrations.
asyncpg==0.31.0
redis>=7.4.0  # app/redis_client.py imports redis.asyncio (rate limits, broadcasts)
psycopg2-binary==2.9.12
greenlet==3.2.4
pydantic==2.13.4
pydantic-settings==2.14.1
email-validator==2.3.0
python-dotenv==1.0.1
httpx==0.28.1
pandas==2.2.3
numpy==2.2.1
ta==0.11.0
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
coinbase-advanced-py==1.3.0
pybit>=5.8.0
anthropic==0.74.0
google-generativeai==0.8.3
openai==2.8.1
web3==7.6.0
eth-account==0.13.7
aiohttp==3.11.10
APScheduler>=3.10.0
feedparser==6.0.12
trafilatura==2.0.0
Pillow==11.0.0

# Development & testing tools
alembic>=1.17.0
flake8>=7.0.0
pytest>=9.0.0
httpx2>=2.3.0  # starlette 1.x testclient prefers httpx2 (httpx use is deprecated there)
pytest-asyncio>=1.4.0  # 1.4.0 fixes an internal unclosed-event-loop leak (ResourceWarning under -W error)
pytest-cov>=7.0.0

# Text-to-speech
edge-tts>=6.1.0

# TOTP MFA (Two-Factor Authentication)
pyotp>=2.9.0
qrcode[pil]>=7.4

# Email via Amazon SES
boto3>=1.35.0

# PDF generation for reports
fpdf2>=2.8.0

# Markdown rendering for AI summaries
markdown>=3.5

# System resource introspection (dynamic concurrency scaling)
psutil>=6.0.0
